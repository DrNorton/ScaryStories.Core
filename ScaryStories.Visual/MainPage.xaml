<phone:PhoneApplicationPage
    x:Class="ScaryStories.Visual.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="clr-namespace:ScaryStories.Visual.Converters"
    mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="800" 
        xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
        FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait"  Orientation="Portrait"
    shell:SystemTray.IsVisible="False">
    
    <phone:PhoneApplicationPage.Resources>
        <converters:ByteToImageConverter x:Key="ByteToImageConverter"/>
        <converters:ByteArrayToImageSource x:Key="ByteArrayToImageSource"/>
    </phone:PhoneApplicationPage.Resources>


    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot">
     
        <controls:Panorama Title="Страшные истории" >
            <controls:Panorama.Background>
                <ImageBrush Stretch="Fill"  ImageSource="{Binding WallpaperImage,Converter={StaticResource ByteArrayToImageSource}}"/>
            </controls:Panorama.Background>

            <controls:PanoramaItem Header="Меню" x:Name="MenuSection">
                <ListBox Margin="0,0,-12,0" ItemsSource="{Binding MenuContexts}" SelectedItem="{Binding SelectedMenuItem,Mode=TwoWay}"  SelectionChanged="Selector_OnMenuSelectionChanged">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                         <Grid>
                             <Grid.ColumnDefinitions>
                                 <ColumnDefinition Width="3*"/>
                                 <ColumnDefinition Width="7*"/>
                             </Grid.ColumnDefinitions>
                           
                                <Image Source="{Binding ImagePath}" Grid.Column="0" />
                                <StackPanel Width="311" Margin="8,-7,0,0" Grid.Column="1">
                                    <TextBlock Text="{Binding Header}" TextWrapping="Wrap" Margin="10,0" Style="{StaticResource PhoneTextExtraLargeStyle}" FontSize="{StaticResource PhoneFontSizeLarge}" />
                                    <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Margin="10,-2,10,0" Style="{StaticResource PhoneTextSubtleStyle}" />
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
           

            </controls:PanoramaItem>
            <!--Panorama item one-->
            <controls:PanoramaItem Header="Категории" x:Name="CategorySection">
                <!--Double line list with text wrapping-->
				<ListBox Margin="0,0,-12,0" SelectedItem="{Binding SelectedCategory,Mode=TwoWay}"  ItemsSource="{Binding Categories,Mode=OneWay}" BorderBrush="{x:Null}" Background="{x:Null}" SelectionChanged="Selector_OnCategorySelectionChanged">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <toolkit:WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="17,0,0,17">
                                <toolkit:HubTile  Source="{Binding Image,Converter={StaticResource ByteToImageConverter}}" Title="{Binding Name}" />
                            </StackPanel>
                            
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                </controls:PanoramaItem>
            <!--Panorama item one-->
            <controls:PanoramaItem Header="Избранное" x:Name="FavoriteSection">
                <ListBox ItemsSource="{Binding FavoriteStories}" SelectedItem="{Binding SelectedFavoriteStory,Mode=TwoWay}" SelectionChanged="Selector_OnFavoriteSelectionChanged" >
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <toolkit:WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                       <DataTemplate>
                            <Grid Margin="17,0,0,17">
                               <Grid.ColumnDefinitions>
                                   <ColumnDefinition Width="4*"/>
                                    <ColumnDefinition Width="6*"/>
                               </Grid.ColumnDefinitions>
                                <Image Grid.Column="0"  Source="{Binding Image,Converter={StaticResource ByteToImageConverter}}"/>
                                <TextBlock TextWrapping="Wrap" Grid.Column="1" Text="{Binding Name}" Style="{StaticResource PhoneTextExtraLargeStyle}" FontSize="{StaticResource PhoneFontSizeLarge}"/>
                           </Grid>
                      
                       </DataTemplate>
                   </ListBox.ItemTemplate>
               </ListBox>
            </controls:PanoramaItem>
        </controls:Panorama>
    </Grid>
</phone:PhoneApplicationPage>